<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.qna.QnaSQLMapper">
    <!-- 질문 답변 게시판 목록 -->
    <select id="getQnaList" resultType="QnaVo">
        select * from eden_qna_board;
    </select>
</mapper>